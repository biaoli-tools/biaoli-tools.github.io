{
  "name": "biaoli-tools",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run build && npm test",
    "serve": "python3 -m http.server 4173 -d _site"
  },
  "engines": {
    "node": ">=20"
  }
}
